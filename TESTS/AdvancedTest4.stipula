/*
The first event is unreachable 
*/

stipula Wrong {
	fields x
	agreement(A){
		A: x
	} => @Init

	@Init A:f()[] {
		now >> @Run {} => @End
	} => @Cont
			
	@Init B:g()[] {
		now + 1 >> @Cont {} => @Run
	} => @Cont

	@Init A:h()[] {
		now + 3 >> @Cont {} => @Run
		now + 5 >> @Cont {} => @Run
	} => @Cont

	@Init A:i()[] {} => @Cont
}
